fastapi==0.115.6
uvicorn[standard]==0.32.1
sqlalchemy==2.0.36
psycopg[binary]==3.2.3
alembic==1.14.0
pydantic==2.10.3
pydantic-settings==2.7.0
email-validator==2.2.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
PyJWT==2.10.1
python-dotenv==1.0.1
python-multipart==0.0.20
httpx==0.28.1
redis==5.2.1

# --- Document Intelligence / RAG (Phase 3) ---
pgvector==0.5.0
pypdf==6.16.2
python-docx==1.2.0
sentence-transformers==6.0.1

# --- Test Suite (Phase 7) ---
pytest==8.3.4
pytest-cov==6.0.0
respx==0.22.0

# --- Server-generated PDF reports (Advanced PMO engines) ---
# reportlab chosen over weasyprint: pure-Python (no native/system dependency like weasyprint's
# GTK/Pango/Cairo stack, which is painful on Windows dev machines and adds real image size in
# Docker), and this app's reports are already structured heading/body/table data (app/schemas/
# report.py) rather than HTML/CSS that would benefit from weasyprint's HTML rendering.
reportlab==4.2.5
